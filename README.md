build.js creates an html blog based on md files in the content folder.
run build.js then server.js to view the page.
Index.html will always be regenerated based on build.js and style.css, so don't edit that file to make changes.  
